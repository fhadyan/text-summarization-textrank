 The constraint a predicate imposes on its arguments is called selectional restriction.  Examples of the output of the automaton are shown in Table .  Now we will specify how we estimate each term in equation .  We will now determine the complexity of the algorithm. 