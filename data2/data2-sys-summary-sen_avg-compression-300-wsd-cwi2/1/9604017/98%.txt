 The specialized grammar used factor New scheme, and had been trained on factor full training set After each level, constituent pruning is used to eliminate unlikely constituents The two methods, constituent pruning and grammar specialization, are combined as follows Constituent pruning factorn removes all sufficiently unlikely edges Finally, factor specialized grammar is used to search for full parses Section describes factor constituent pruning method (Recall that grammar specialization in general trades coverage for speed .