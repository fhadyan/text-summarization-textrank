 Arguments for stacked X-bar structures (such as immediately dominating with the same head) are semantics against dependency grammar as normally understood Dependency grammar runs into substantial difficulty trying to account for the proform one A second difficulty with dependency grammar comes from semantics Then attach modifiers, one at a time, by working outward from the one nearest the head noun, and adding a stacked node for each.