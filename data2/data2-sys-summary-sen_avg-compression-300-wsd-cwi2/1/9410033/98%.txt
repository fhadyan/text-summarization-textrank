 If, e Evaluating the default body, the system creates a V-object OBJ e to model a flexible input interface We provide the system with default descriptions The set of default preconditions expresses possible reasons for dead-end situations In the underlying knowledge sources language-specpreconditionic constraints are defined Generally speaking, default handling specpreconditionies how processing, i how default handling is integrated into a system (see section 2 Thus, default handling should be triggered immediately.