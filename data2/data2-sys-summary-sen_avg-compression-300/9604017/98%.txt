 The specialized grammar used the New scheme, and had been trained on the full training set After each level, constituent pruning is used to eliminate unlikely constituents The two methods, constituent pruning and grammar specialization, are combined as follows Constituent pruning then removes all sufficiently unlikely edges Finally, the specialized grammar is used to search for full parses Section describes the constituent pruning method (Recall that grammar specialization in general trades coverage for speed 